# Заменяем встроенное представление процессов EvaTeam на продвинутое

В [EvaTeam](https://evateam.ru) на странице задач вроде https://eva.gid.team/project/Task/DATA-5945 есть кнопка отображения Workflow.

По ней открывается схема, которая выглядит примерно так: makets/maket-workflow.as-is.png.
HTML код данного блока (видимо без стилей) представлен в файле makets/maket.as-is.html.

Необходимо написать tampermonkey user.js script в файле evateam-workflow-enhance.user.js, который преобразовывает это представление и модернизирует его!

# Основные требования:
1. Все элементы должны быть перетаскиваемыми (dragable).
2. Предлагаю использовать для отображения [reactflow](https://reactflow.dev).
3. Изначально нужно предложить автоматическое расположение блоков, так чтобы блоки по возможности не пересекались и диаграмма была бы читаемой.
4. Если пользователь поменял расположение блоков, это должно сохраняться в локальном хранилище браузера (например localStorage) и восстанавливаться при последующем открытии этого же воркфлоу.
   * При этом стоит сохранять какой-то хеш воркфлоу, и если он изменится подтянуть изменения, даже если это где-то изменит положения
   * Постараться максимально сохранить то что было вручную упорядочено пользователем (не двигать блоки, которые не менялись).
5. Пример макета можно посмотреть в файле makets/maket.to-be.png.
   * В целом, цвета лучше оставить в цветовой гамме EvaTeam
   * Но для блоков и стрелок можно использовать дополнительные цвета, соблюдая цветовую гамму
   * Подписи стрелок и блоков берём из исходной диаграммы
6. В Tampermonkey-скрипте зависимости подкгружаем через директиву @require в заголовке, указывая ссылку на CDN.
7. Страницы на которых должен работать скрипт: `@match https://eva.gid.team/project/Task/*`
8. Исходное отображение, должно остаться и быть доступно на вкладке (tab) "Исходный workflow"
   * Должна быть возможность переключения пользователя между вкладками.
9. Если структура исходного workflow на странице окажется неожиданной или повреждённой, скрипт не должен "падать". В этом случае следует отобразить сообщение об ошибке на месте новой диаграммы, с возможностью показать вкладку с исходным workflow.

# РЕАЛИЗОВАННЫЕ ФУНКЦИИ (v0.2) ✅

## Основные возможности
- ✅ Полноценная интеграция с ReactFlow
- ✅ Перетаскивание узлов с автосохранением позиций
- ✅ Переключение между улучшенным и исходным отображением
- ✅ Адаптивный дизайн и обработка ошибок

## Продвинутые функции
- ✅ **Автоматическая раскладка узлов** - интеллектуальное размещение по слоям
- ✅ **Персистентность данных** - сохранение позиций в localStorage
- ✅ **Хеширование workflow** - отслеживание изменений структуры
- ✅ **Умное восстановление макета** - сохранение пользовательских настроек
- ✅ **Управление раскладкой** - кнопки сброса и очистки
- ✅ **Обработка ошибок** - graceful degradation и информативные сообщения

## Технические улучшения
- ✅ Производительность - оптимизированный алгоритм авто-раскладки
- ✅ Надежность - валидация данных и обработка edge cases
- ✅ UX - индикаторы загрузки и состояния
- ✅ Отладка - подробное логирование с префиксом "Hu: EvaTeam Workflow Enhancer:"

# АЛГОРИТМ АВТО-РАСКЛАДКИ

Скрипт использует улучшенный алгоритм топологической сортировки:
1. **Анализ связей** - определяет входящие и исходящие связи для каждого узла
2. **Поиск начальных узлов** - находит узлы без входящих связей (источники)
3. **Рекурсивное размещение** - размещает узлы по уровням от источников
4. **Обработка изолированных узлов** - случайное размещение узлов без связей
5. **Оптимизация позиций** - минимизация пересечений связей

# СОХРАНЕНИЕ И ВОСТАНОВЛЕНИЕ МАКЕТА

## Структура данных localStorage
```javascript
{
  workflowId: "DATA-5945",
  timestamp: 1704123456789,
  nodes: [
    {
      id: "node_start",
      position: { x: 50, y: 20 }
    },
    // ... остальные узлы
  ]
}
```

## Логика работы
1. **При загрузке** - проверяет наличие сохраненного макета для workflow ID
2. **Хеширование** - сравнивает структуру workflow с сохраненной версией
3. **Восстановление** - применяет сохраненные позиции если структура не изменилась
4. **Авто-раскладка** - пересчитывает позиции при изменении структуры
5. **Автосохранение** - сохраняет изменения через 1 секунду после последнего редактирования

# КНОПКИ УПРАВЛЕНИЯ

## 🔄 Авто-раскладка
- Пересчитывает позиции всех узлов по алгоритму топологической сортировки
- Сохраняет результат в localStorage
- Полезно для улучшения читаемости сложных диаграмм

## 🗑️ Очистить макет
- Удаляет сохраненный макет из localStorage
- Применяет авто-раскладку заново
- Сбрасывает все пользовательские настройки позиционирования

# СОВМЕСТИМОСТЬ И ТРЕБОВАНИЯ

## Браузеры
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

## Зависимости
- React 18 (загружается из unpkg.com)
- ReactFlow 11 (загружается из unpkg.com)
- Современный браузер с поддержкой ES6+

## Ограничения
- Требует стабильную структуру DOM EvaTeam
- Зависит от доступности CDN библиотек
- localStorage должен быть доступен (не в приватном режиме)

# ОТЛАДКА И МОНИТОРИНГ

Все операции логируются в консоль браузера с префиксом:
```
Hu: EvaTeam Workflow Enhancer: [сообщение]
```

## Уровни логирования
- **INFO** - общая информация о работе
- **WARN** - предупреждения о потенциальных проблемах
- **ERROR** - критические ошибки с деталями

## Полезные команды для отладки
```javascript
// Очистить все сохраненные макеты
Object.keys(localStorage).filter(key => key.startsWith('evateam_workflow_layout_')).forEach(key => localStorage.removeItem(key));

// Показать сохраненные макеты
Object.keys(localStorage).filter(key => key.startsWith('evateam_workflow_layout_')).forEach(key => console.log(key, JSON.parse(localStorage.getItem(key))));

// Принудительно пересчитать макет
localStorage.removeItem(`evateam_workflow_layout_${getWorkflowIdFromUrl()}`);
location.reload();
```
